COMMISS POOL — ESPN QUERY-FORMAT DIAGNOSTIC

This build is diagnostic only. It does NOT change scoring, picks, games, or live pool logic.

It tests Stanford-Duke (ESPN event 401858231) against four college scoreboard query forms:
1. Exact date
2. Exact date + FBS groups=80
3. Week 3 + seasontype=2 + FBS groups=80
4. Week 3 + seasontype=2

The app displays the result and event count in the feed status bar.
